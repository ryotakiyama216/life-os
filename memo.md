# 現状ステータス（引き継ぎメモ・2026-08-29時点）

このセクションは次にこのファイルを開いたとき（自分 or 別セッションのClaude）が状況を即座に把握できるようにするための引き継ぎメモ。下の「UI/UX改善メモ」チェックリスト本体は編集せず、状況が変わったらこのセクションだけ更新すること。

## 今どこまで進んでいるか

- ブランチ `ui-ux-improvements` で作業中。**まだ1つもコミットしていない**（`git status`で49ファイルがuncommitted）。mainにマージもpushもしていない。
- 下記チェックリストの15項目は**すべて実装済み**（`[x]`）。`npm run build`成功（型チェック・ESLint通過）。devサーバーでの主要ページのHTTP応答確認と、ユーザー自身によるブラウザ実クリック確認（ログイン→画面遷移）も完了。
- 次にやるべきことは「コミットするかどうか」をユーザーに確認すること。このセッションでは指示がなかったため未コミットのまま。ユーザーから明示的な指示があれば、変更をコミット（新規ファイル: `history/2026-08-27.md`, `src/components/breadcrumb.tsx`, `src/components/confirm-delete-dialog.tsx`, `src/components/overdue-label.tsx`, `src/components/today/today-skeleton.tsx`, `src/components/ui/alert-dialog.tsx`, `src/components/ui/skeleton.tsx`）→ mainへのマージ/PR作成、の順で進める。

## このセッションで判明した環境まわりの注意点

- **`.env.local`がリポジトリに存在しなかった**（`.env.local.example`のみ）。`node_modules`も未インストールだった。おそらくこのマシン/checkoutでは初回セットアップが未実施だった。`npm install`を実行し、`.env.local`を`.env.local.example`からコピーしてユーザーに値を入力してもらった（Supabase URL + 新形式の`sb_publishable_...`キー。旧来のJWT形式anonキーより短いが正常）。
- **ログインが失敗する不具合が発生したが、原因はブランチの変更ではなく環境要因だった**: devサーバーを`.env.local`追加後に再起動しても、ブラウザに開きっぱなしのタブは古いJSバンドル（Supabase接続情報なしの状態）をキャッシュしたままで、ログインボタンが「処理中...」のまま固まって見えた。**ハードリロード（Cmd+Shift+R）で解決**。サーバー側（Supabase認証エンドポイントの疎通・CORS・APIキーの有効性）はcurlで全て正常と確認済みだったので、原因切り分けの参考に。
- Node.jsは実際には20.19.6が入っている（CLAUDE.mdの「ローカルNode.jsが18.17系」という記載は古い可能性がある。Next.js 14系を使う理由自体は変わらず妥当）。

## 次にやること（優先度順）

1. **このブランチの扱いをユーザーに確認**: コミットしてよいか、mainにマージ/PRにするか、それともこのまま作業を続けるか。
2. `supabase/schema.sql`末尾の`events`テーブルをSupabase SQL Editorで実行済みか未確認（CLAUDE.mdに以前から記載されている積み残し）。`/schedule`ページは今回200応答は確認したが、実データでのエラーの有無は未検証。心配なら`events`テーブルが実際に存在するかユーザーに確認してもらう。
3. Vercelデプロイ（環境変数設定）はまだ未着手。
4. AI機能（棚卸し自動化）はコスト判断待ちで保留中（勝手に着手しない。API課金体系の確認が先）。
5. SNS管理機能は未着手（詳細要件は未ヒアリング）。

---

# UI/UX改善メモ（2026-08-27 Opus監査分）

新規ブランチでの修正作業用チェックリスト。完了したら`[x]`にする。
詳細な経緯はこのセッションのhistory記録も参照。

## 最優先: Todayページと基本的な視認性

- [x] 1. 初回ロード時のローディング状態欠如（影響:大 工数:中）
      `useAppStore`の`loading`/`loaded`をどのページも参照していない。`loadAll()`完了までEmptyStateが一瞬フラッシュする。
      → `loaded===false`の間はスケルトン表示（Today中心）。

- [x] 2. Todayでタスク完了が2アクション必要（影響:大 工数:小）
      `TaskStatusControl`はバッジ→ドロップダウン→「完了」の2タップ。詳細ページはチェックボックス1タップで不統一。
      → `TaskItem`先頭にチェックボックスを追加し1タップで`completeTask`。

- [x] 3. Todayのモバイル縦積み順が「今すべきこと」優先でない（影響:大 工数:小）
      `(app)/page.tsx`のグリッドはモバイルで「習慣→タスク→時間割」の順。最重要のタスクが沈む。
      → モバイルでは中央カラム（overdue/todayタスク/候補）を先頭に`order`で並べ替え。

- [x] 4. 優先度・状態の色が氾濫し「本当の緊急」が埋もれる（影響:中 工数:小）
      `priority-badge.tsx`（P1赤/P2アンバー/P3青/P4グレー）と`status-badge.tsx`（in_progress青など）で色の意味が重複。
      → P3/P4はニュートラルに、赤はP1・締切超過のみに限定。in_progressは色でなく左ボーダー等で表現。

## 情報設計・階層の可視化

- [x] 5. 目標・プロジェクトの進捗が数値のみでバーがない（影響:中 工数:小）
      `goal-card.tsx`/`project-card.tsx`は「done/total」テキストのみ。
      → 細い進捗バー（`h-1 rounded bg-muted` + `bg-primary`）を追加。

- [x] 6. タスク詳細からプロジェクト/目標への導線がない（影響:中 工数:小）
      `tasks/[id]/page.tsx`はSelectで変更できるが遷移リンクがない。
      → Selectラベル横に「開く↗」リンクを追加。

- [x] 7. 一覧・詳細ページにパンくずがない（影響:小 工数:小）
      「〜一覧へ」戻るボタンのみで階層位置が見えない。
      → 目標/プロジェクト/タスク詳細ページに`Breadcrumb`コンポーネントを追加し目標→プロジェクトのパスを表示。

## フィードバック・入力体験

- [x] 8. 削除が確認なしで即実行される（影響:中 工数:小）
      `task-item.tsx`等で確認ダイアログなしに`removeTask`。
      → shadcnのalert-dialogを追加して破壊操作前に確認。

- [x] 9. フォームの必須/任意表記とバリデーション表示が弱い（影響:中 工数:小）
      必須Labelに`*`がなく、エラーはtoastのみでフィールド近傍に出ない。
      → 必須マーク追加、送信不可理由をフィールド下に表示。

- [x] 10. overdue表現がコンポーネントごとにバラバラ（影響:小 工数:小）
      `focus-suggestions.tsx`だけ独自の赤テキストで他と不整合。
      → 共通`OverdueLabel`コンポーネントに切り出し共有。

## ナビゲーション・一貫性

- [x] 11. サイドバーがフラットで12項目（影響:小 工数:小）
      主動線と補助機能が混在。
      → `Separator`や小見出しでグルーピング、設定を最下部へ。

- [x] 12. 一覧ページに件数・サマリが出ない（影響:小 工数:小）
      Todayは件数付きだが他の一覧は非対称。
      → PageHeaderに「表示中 N件」を追加。

- [x] 13. ボタンサイズ・アクション配置の不統一（影響:小 工数:小）
      primary/outline/ghostの使い分け、アイコンサイズがページごとにバラバラ。
      → ボタン階層ルールを統一。

## アクセシビリティ

- [x] 14. フォーカスリング・aria属性の不足（影響:中 工数:小）
      `TaskStatusControl`トリガー等に`focus-visible`スタイルなし、aria-label未付与箇所あり。
      → インタラクティブ要素に`focus-visible:ring-2 ring-ring`とaria-labelを付与。

- [x] 15. 「直近の予定」カードが最重要情報を押し下げる（影響:小 工数:小）
      `(app)/page.tsx`で`UpcomingEvents`が全幅で最上部を占有。
      → 右カラムへ移動。
