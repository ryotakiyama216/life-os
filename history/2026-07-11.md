# 2026-07-11 Life OS 開発記録

## 今日やったこと概要
要件ヒアリングを行い、MVP（見た目＋localStorage版）を一気通貫で構築した。Next.js 14 + shadcn/ui + Zustand(localStorage永続化) で、GTD方式のTodo管理ツールの土台が動く状態になった。

## ヒアリングで決まったこと

### プロダクトの狙い
- 目標(Goal) → プロジェクト(Project) → タスク(Task) → 今日のTodo、の階層で管理。ただしプロジェクトを経由しない目標や、習慣だけで達成する目標もあり得る前提。
- Notionは多機能すぎて一人運用だと使わなくなる、という課題感があるため「シンプルさ」を最優先。
- GTD（Getting Things Done）の考え方を採用。緊急タスクや目標と無関係な予定も扱えるようにする。
- 「今、目標達成のために何をすべきか」だけを考えられる状態を目指す。期限切れタスクの自動検出・誘導、日付が今日のタスクの自動表示など、Notionで自分では設定しきれなかった部分を最初から仕組み化する。
- 将来的にSNS投稿・伸びの分析機能も追加したい（今回は着手せず）。

### 技術・認証・AI
- フロントエンド: Next.js、バックエンド/DB: Supabase、ホスティング: Vercel。
- 認証は自分専用（1人運用）でよい。将来Supabase Authを使う想定だが今回は未実装。
- AI機能（チャット/アシスタント、目標→プロジェクト→タスクの自動棚卸し、毎日/毎週のレビュー生成）はやりたいが、**今回のMVPには含めない**。Claude課金を活用してツール内AIを実装したいという要望あり（次フェーズ）。

### 今回のセッションのスコープ判断
当初はTodayビューまでフル実装する想定だったが、ヒアリングの結果「まず見た目とローカルストレージでの動作確認を優先し、DB接続は次回以降」という方針に変更。これにより認証・Supabase構築を待たずにUI/UXを検証できるようにした。

### データ設計の方針
- カラムは固定（Notionのような自由追加式プロパティではない）。ただし将来のSupabaseスキーマにそのまま移行できる型設計にした。
- 優先度は P1〜P4（GTD/Notion的な4段階）。
- 朝のスケジュールは「時間割ブロック形式」（時刻＋タイトルのブロックを追加・並び替えできる構造化データ）。

## 実装した内容

### 技術スタック（今回選定）
- Next.js **14系**（App Router, TypeScript, src/ディレクトリ構成）
  - 理由: ローカルのNode.jsが v18.17.0 / v18.9.0 系で、Next.js 15以降が要求する Node 18.18+ / 20+ を満たしていなかったため。将来Node 20にアップグレードした際にNext.js最新版へ移行可能。
- Tailwind CSS + shadcn/ui（style: new-york, baseColor: zinc＝モノトーン）
  - shadcn CLIの最新版（4.x系）もNode 20+必須で動かなかったため、`shadcn@2.3.0` を使用。
  - `tailwind.config.ts` のカラー定義が `hsl(var(--x))` 形式だったが、globals.cssの生成値が `oklch(...)` 関数そのものだったため不整合が発生 → `var(--x)` 直接参照に修正済み（重要な既知バグの回避、次回shadcn関連の変更をする際は要注意）。
- 状態管理・永続化: Zustand + `persist`（localStorage、キー: `life-os-store`）。将来Supabase化する際はstoreのCRUDメソッドの中身だけ差し替えればよい設計。
- Markdown: `react-markdown` + `remark-gfm` + `@tailwindcss/typography`（`prose`クラス）。編集はシンプルなtextarea＋プレビュー切り替え（重量級エディタは導入せず）。
- テーマ: `next-themes`（ライト/ダーク、モノトーン基調を両対応）。

### データモデル（`src/types/index.ts`）
Goal / Project / Task / Habit / HabitLog / InboxItem / Note / MorningBlock を定義。将来のSupabaseテーブル設計にほぼそのまま流用できる想定。

### 主要機能
- **Today (`/`)**: 期限切れタスクの警告表示＋今日やる/明日に延期/削除などのアクション、今日期限・今日予定のタスク一覧、「今やるべき候補」（目標/プロジェクトの優先度から逆算したフォーカス提案 = `src/lib/priority.ts`）、今日の習慣チェックリスト、朝の時間割ブロック。
- **Inbox (`/inbox`)**: クイックキャプチャで書き溜め、各アイテムをタスク/プロジェクト/目標/習慣/メモ/いつかリストに仕分け。
- **目標 (`/goals`, `/goals/[id]`)**: 状態・優先度でのフィルタ/ソート、詳細ページでMarkdown説明＋紐づくプロジェクト/タスクの管理。
- **プロジェクト (`/projects`, `/projects/[id]`)**: 同様にフィルタ/ソート＋タスク一覧。
- **タスク (`/tasks`)**: 状態・優先度・目標・プロジェクト・キーワードでの絞り込み、期限日/優先度/状態/作成日での並び替えができるテーブルビュー。
- **習慣 (`/habits`)**: 頻度（毎日/曜日指定）と実施時刻の設定。Todayページに自動反映。
- **ページ・メモ (`/notes`, `/notes/[id]`)**: Markdownで自由に書けるメモ。目標/プロジェクトへの関連付け、タグ付け可能。

### 優先度エンジン（`src/lib/priority.ts`）
1. 期限切れを最優先表示（超過日数で強調度アップ）
2. 今日期限・今日予定のタスク
3. 日付未設定でも、紐づく目標・プロジェクトの優先度(P1〜P4)を合成したスコアで「今やるべき候補」を提示 → 好きなタスクに流されず、目標から逆算した優先順位を機械的に提示する仕組み。

## 動作確認
- `npm run build` 成功（型チェック・ESLintエラーなし）。
- `npm run dev` を起動し、`/`, `/tasks`, `/notes`, `/goals`, `/goals/[id]`, `/projects`, `/projects/[id]`, `/inbox`, `/habits`, `/notes/[id]` の主要ルートがすべて200を返すことを確認。SSR出力にサイドバー・テーマスクリプト・スタイルが正しく含まれていることも確認済み。
- ブラウザでの実クリック操作の確認はしていない（自動ブラウザ操作ツールがない環境のため）。次回、実機で以下を確認してほしい:
  - クイックキャプチャ→Inbox仕分け→Today表示→タスク完了の一連の流れ
  - ダークモード切替
  - モバイル幅でのドロワーメニュー

## 既知の制約・次回への申し送り
- **DB未接続**: すべてlocalStorage（ブラウザ単位）。別ブラウザ/別端末とはデータが同期されない。
- **認証なし**: 誰でもアクセスできる状態（今はローカル開発のみなので問題なし、Vercelにデプロイする前にSupabase Auth導入が必要）。
- **AI機能未実装**: チャット/アシスタント、目標→プロジェクト→タスクの自動棚卸し、レビュー自動生成は次フェーズ。
- **SNS管理機能**: 将来構想として未着手。

## 次回セッションでやること（候補）
1. 実機ブラウザでのUI/UX確認・フィードバック反映
2. Supabaseプロジェクトの作成（未作成とのことだったので、作成手順から）とスキーマ設計（`src/types/index.ts` をベースにテーブル化）
3. localStorageストア → Supabase読み書きへの差し替え（Zustand storeのアクション実装だけ差し替える設計にしてある）
4. Supabase Authでの自分専用ログイン導入
5. Vercelへのデプロイ設定
6. AI機能（チャット/アシスタント、棚卸し、レビュー生成）の設計・実装

## 追記: AI機能「目標→プロジェクト/タスクの自動棚卸し」を追加

### ヒアリングで確認したこと
- **Claude.aiのチャット課金とAnthropic APIは別課金**であることを説明。ユーザーはまだAPIキー未発行だったため、console.anthropic.comでの発行手順を案内。
- 最初に実装するAI機能は「目標→プロジェクト/タスクの自動棚卸し」（3案のうち最優先として選択）。
- モデルはコストと品質のバランスから **Claude Sonnet 5**（`claude-sonnet-5`）を選定。個人ツールで頻繁に呼ぶ想定のため、Opus 4.8（単価5倍）は見送り。

### 実装内容
- `@anthropic-ai/sdk` と `zod` を追加。
- `src/lib/anthropic.ts`: サーバー専用のAnthropicクライアント（`ANTHROPIC_API_KEY`が未設定なら例外）。
- `src/app/api/ai/breakdown/route.ts`: POSTエンドポイント。目標のタイトル・説明を受け取り、`client.messages.parse()` + `zodOutputFormat`で構造化出力（プロジェクト案＋直接タスク案、それぞれ優先度P1〜P4付き）を返す。APIキー未設定時は日本語でわかりやすいエラーを返す。
- `src/components/goal/ai-breakdown-dialog.tsx`: 目標詳細ページに置く「AIで棚卸しする」ボタン。ダイアログを開くと自動でAI呼び出し→提案されたプロジェクト/タスクをチェックボックスで選択→選んだものだけをZustand storeに追加。
- `src/app/goals/[id]/page.tsx` にボタンを配置。
- `.env.local.example` を追加（`ANTHROPIC_API_KEY=`）。README.md・CLAUDE.mdにAPIキー取得手順とAI実装方針を追記。

### 動作確認
- `npm run build` 成功（`/api/ai/breakdown` ルートが認識されている）。
- APIキー未設定の状態で `POST /api/ai/breakdown` を叩き、想定通り日本語のエラーメッセージ（500）が返ることを確認。
- **実際にAPIキーを設定した状態でのAI応答の中身（提案内容の質）はまだ確認していない** — ユーザーがAPIキーを設定次第、実機で試してほしい。

### セッション中に発生したトラブル
- 途中でMacのディスク容量が完全に枯渇し、Bashコマンドが一切実行できなくなった（`ENOSPC: no space left on device`）。ユーザーに空き容量確保を依頼し、解消後に作業再開。今後もnode_modulesの再インストールなどでディスクを圧迫しやすいので、容量に注意。

### 次回への申し送り（この後、AI機能は取り下げ済み。下の追記を参照）

## 追記2: AI機能を一旦取り下げ

ユーザーより「Claude.aiの課金とは別にAPI課金が発生するなら、AI機能は一旦やめたい」との判断。以下を実施しMVP（AI機能なし）の状態に戻した。

- 削除: `src/app/api/ai/`（Route Handler一式）、`src/lib/anthropic.ts`、`src/components/goal/ai-breakdown-dialog.tsx`、`.env.local.example`
- `src/app/goals/[id]/page.tsx` から「AIで棚卸しする」ボタンの配置を削除
- `npm uninstall @anthropic-ai/sdk zod`
- README.md / CLAUDE.md からAI関連の記述を削除（今後の予定には「コスト面の判断が必要なため保留中」と記載）
- `npm run build` 成功を確認、AI機能追加前の状態に戻ったことを確認済み

### 次回への申し送り（この後、Supabase接続を実施。下の追記を参照）

## 追記3: Supabase（DB + 認証）への移行

ユーザーより「localStorageではなくDBに保存していきたい」との指示。ヒアリングの結果、Supabaseプロジェクトはユーザーが`supabase.com`で自分で作成してURL/anon keyを渡す方式、認証（Supabase Auth・自分専用ログイン）も同時に導入することに決定。

### 実装内容
- `@supabase/supabase-js` + `@supabase/ssr` を導入（旧`auth-helpers-nextjs`ではなく現行推奨パターン）
- `src/lib/supabase/client.ts`（ブラウザ用）、`src/lib/supabase/server.ts`（サーバー用）、`src/middleware.ts`（セッションリフレッシュ+未ログイン時`/login`リダイレクト）
- `supabase/schema.sql`: goals/projects/tasks/habits/habit_logs/inbox_items/notes/morning_blocksの全テーブル、RLS（`auth.uid() = user_id`）、`user_id default auth.uid()`（クライアントからuser_idを明示しなくて良い設計）、updated_at自動更新トリガー。ユーザーがSupabase SQL Editorで手動実行する運用（マイグレーションツールなし）
- `src/lib/supabase/case.ts`: snake_case⇄camelCaseの汎用変換。`src/lib/supabase/entity.ts`: `createEntityQueries<T>(table)`という汎用CRUDファクトリを作り、`src/lib/supabase/queries/`配下の各エンティティファイルはほぼこれを呼ぶだけの薄い実装に抑えた（habit_logsのみtoggle用の個別ロジック）
- `src/store/useAppStore.ts`を全面書き換え: `persist`（localStorage）を撤去し、`loadAll()`でSupabaseから読み込み、各CRUDアクションを非同期化（失敗時は`toast.error`を出してthrow）
- `src/components/auth/auth-provider.tsx`（セッション監視、ログイン後に`loadAll()`実行）、`src/app/login/page.tsx`（ログイン/新規登録フォーム、自分でアカウント作成できるためパスワードを私に共有する必要はない設計）、Topbarにログアウトボタン追加
- ルーティング再構成: 既存ページを`src/app/(app)/`route groupへ移動、`(app)/layout.tsx`でAppShellを描画。ルートlayout.tsxはAppShellを持たずAuthProvider/ThemeProvider/Toasterのみに変更。`/login`はAppShellの影響を受けない独立ページ
- `notes/[id]/page.tsx`: タイトル・本文編集がキー入力ごとにDB書き込みされないよう、`useDebouncedCallback`（600ms）を新設して導入
- フォームダイアログ（task/goal/project/habit）とinbox仕分け、notes新規作成、goal/project削除の呼び出し箇所をasync化し、ストアの非同期アクションを`await`するよう修正
- `.env.local.example`を`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`向けに更新。README.md/CLAUDE.mdにSupabaseセットアップ手順・アーキテクチャ変更点を追記

### 動作確認
- `npm run build`成功を確認（型エラー・ESLintエラーなし）
- **実際のSupabase URL/anon keyはまだ受け取っていないため、実データでのサインアップ→ログイン→CRUD操作の動作確認はまだ行っていない**。ユーザーが`.env.local`を設定し、`supabase/schema.sql`をSupabase側で実行した後、実機で確認してもらう必要がある

### 次回への申し送り（この後、実際にSupabase接続・動作確認まで完了。下の追記を参照）

## 追記4: Supabase実接続の確認 / アカウント設定ページ / 習慣並び替え・Todayレイアウト・振り返り画面

### Supabase実接続
- ユーザーがSupabaseプロジェクト（プロジェクト名「LIFE OS」、リージョンAsia-Pacific）を作成し、Project URL・anon keyを共有。DBパスワードは私のメモリには保存せず、リポジトリ直下の`DATABASE_CREDENTIALS.md`（`.gitignore`登録済み・git管理外）に記録する方針にした
- `.env.local`にURL/anon keyを設定し、`supabase/schema.sql`をユーザーがSupabase SQL Editorで実行。全8テーブルの作成をREST API経由で確認
- サインアップ→メール確認リンク→ログインの一連の流れが実際に動作することを確認。ただし`notes`テーブル等が無いエラーが最初出ていたのは、まだschema.sql未実行だっただけで、実行後は解消

### アカウント設定ページ追加
- ユーザーから「ログイン情報をUIで確認・編集したい、名前も」との要望。私はユーザーの実際のログイン情報（メール/パスワード）を一切保持していないことを説明した上で、`/settings`ページを新規実装
- `src/app/(app)/settings/page.tsx`: 名前（`user_metadata.full_name`、`supabase.auth.updateUser({data:{full_name}})`で保存）とメールアドレス（`supabase.auth.updateUser({email})`、変更時は確認メールが必要な旨を表示）を編集できるフォーム
- ナビゲーションに「設定」を追加

### 習慣の並び順・Todayレイアウト・振り返り画面
ユーザーからの改善相談3件に対応:
1. **習慣が実施時刻順に並んでいない** → `src/lib/habit.ts`に`sortHabitsByTime`を新設し、Today画面・習慣管理画面の両方に適用（時刻未設定は末尾）。ドラッグ&ドロップの手動並び替えは今回は見送り（自動ソートで要望を解決）
2. **Todayが2カラムで右側が縦に伸びがち** → 3カラムに再構成（`src/app/(app)/page.tsx`）: 左「今日の習慣」／中央「期限切れ・今日やること・今やるべき候補」／右「今日の時間割」
3. **振り返り画面が欲しい** → `/review`（活動があった日付の一覧、タスク/プロジェクト/習慣の件数サマリ）と`/review/[date]`（その日に完了したタスク・プロジェクト・実施した習慣の詳細）を新規実装。プロジェクトの完了日を正確に記録するため`projects.completed_at`列を追加（`supabase/schema.sql`に追記、ユーザーには追記部分のみSQL Editorで実行してもらう必要あり）。ストアの`updateProject`はstatusが'done'に変わった瞬間に自動でcompletedAtをセットする

### 動作確認
- `npm run build`成功
- devサーバーで`/`と`/review`が未ログイン時に`/login`へリダイレクトされることを確認（middlewareが新ルートでも機能）
- **`projects.completed_at`のALTER文はまだユーザーに実行してもらっていない**。実行しないと、プロジェクトを完了にしても振り返り画面に反映されない

### 次回への申し送り（この後`completed_at`のALTER文はユーザーが実行済み・カラム存在をREST APIで確認済み。下の追記を参照）

## 追記5: Today中央カラム拡幅・タスク詳細ページ・期限切れ表示の修正

実運用フィードバック3件に対応:
1. **Today中央カラムが3カラム化で狭い** → `src/components/layout/app-shell.tsx`のコンテナ幅を`max-w-5xl`→`max-w-7xl`に拡大（全ページに影響、基本的に改善方向）
2. **「今日の予定にする」を押しても期限切れタスクの表示が変わらない（バグかも）** → 原因を特定: `src/lib/priority.ts`の`getTodayTasks`は期限切れタスクをTodayリストと重複させないため意図的に除外する仕様で、データ自体は正しく更新されていた。`TaskItem`/`TaskTableRow`に「今日やる予定」バッジ（`overdue && scheduledDateが今日`の時だけ表示）を追加し、期限切れリスト上でも予定した結果が見えるようにした
3. **タスクの詳細（説明文）を見る手段がない** → `/tasks/[id]`を新規追加（goals/projectsと同じ一覧+詳細パターン、`MarkdownContent`で説明文をレンダリング、編集は既存の`TaskFormDialog`を再利用）。`TaskItem`・`TaskTableRow`のタイトルを詳細ページへの`Link`に変更（タイトルクリックで遷移、ドロップダウンの「編集」はダイアログ直接起動のまま残す）

### 動作確認
- `npm run build`成功
- devサーバーで`/`・`/tasks`が未ログイン時に`/login`へ307リダイレクトされることを確認（middlewareが新ルートでも機能）
- 実際の見た目（余白、バッジ、詳細ページ遷移）はユーザーに確認依頼

### 次回への申し送り（この後さらに2件対応。下の追記を参照）

## 追記6: 「今やるべき候補」の絞り込み・期限切れタスクのアクション明示化

ユーザーから2つの相談:
- 「今やるべき候補」に、すでに実施予定日を設定した未来のタスクまで出てきてしまい、タスクが増えると管理しきれなくなりそう
- 期限切れタスクの「今日やる/予定変更/削除」が「…」ドロップダウンに隠れていて分かりにくい

Q&A: 期限日が今日になったら自動で「今日やること」に入るか→**入る**（`getTodayTasks`が`isToday(dueDate)`を見ている）ことを確認して回答。

対応:
- `src/lib/priority.ts`の`getFocusSuggestions`のフィルタに`!t.scheduledDate`を追加。実施予定日（未来含む）が設定済みのタスクは候補から除外されるようになった（表示件数の5件はそのまま）
- `src/components/task/overdue-task-item.tsx`を新規作成。期限切れタスク専用に「今日やる／明日に延期／編集／削除」を「…」メニューではなくボタンとして直接横並び表示。`src/components/today/overdue-section.tsx`で`TaskItem`から`OverdueTaskItem`に差し替え

### 動作確認
- `npm run build`成功、devサーバーで`/`が未ログイン時に307リダイレクトされることを確認
- 実際の見た目（候補から予定日ありタスクが消えること、期限切れのボタンが直接見えること）はユーザーに確認依頼

### 次回への申し送り（この後さらに大きめの機能追加あり。下の追記を参照）

## 追記7: Todayバグ修正・日付バリデーション・タスク編集ページ刷新・一覧の簡易編集・タグ検索

途中でMacのディスク容量が再び枯渇（`ENOSPC`）。ユーザーに空き容量確保を依頼して解消後、計画を再構成して実装続行。

ユーザーからの複数フィードバック:
- 期限切れタスクを「今日やる」にしても「今日やること」に出てこない → **バグ確定・修正**。原因は`src/lib/priority.ts`の`getTodayTasks`が「dueDateが過去なら無条件除外」していたため。要望は「期限が今日 or 予定日が今日なら自動でToday表示」なので、overdueによる除外条件自体を削除（`isOpenTask(t) && (isToday(t.dueDate) || isToday(t.scheduledDate))`のみに簡素化）。これにより「期限切れだが今日やる予定にした」タスクは期限切れセクションとTodayの両方に出るようになる（意図的な仕様）
- 期限日は予定日より前に設定できないようにする → `TaskFormDialog`・新タスク編集ページ・`/tasks`一覧のインライン編集すべてに、文字列比較によるバリデーション（`dueDate < scheduledDate`なら`toast.error`で中断）＋日付inputへの`min`/`max`属性を追加。**ただし期限切れタスクの「今日やる」クイックボタン（`OverdueTaskItem`）は意図的にこのバリデーション対象外**（すでに期限切れの案件を「今日中にやる」と決める行為なので、期限日より予定日が後になるのは正常なケース）
- 一覧に予定日も表示 → `TaskItem`・`OverdueTaskItem`に「予定 {日付}」表示を追加（今日なら強調）。`/tasks`テーブルに「予定日」列を追加（表示のみ）
- タスク編集をモーダルではなくページ遷移にし、Markdown欄を大きく → `/tasks/[id]`を全面刷新。`/notes/[id]`と同じパターン（`useDebouncedCallback`で自動保存、Select/Inputを直接いじれる、`MarkdownEditor`のminRowsを16→24に拡大）。`TaskFormDialog`は新規作成専用に用途を絞り、`TaskItem`/`OverdueTaskItem`の「編集」はページ遷移に変更
- `/tasks`一覧でタイトル・状態・優先度・期限・目標/プロジェクトをその場で編集したい → `TaskTableRow`を全面刷新。タイトルはインライン入力（デバウンス）＋詳細ページへの小さいリンクアイコン、状態/優先度はセル内`Select`（状態がdoneになる場合は`completeTask`経由でcompletedAtを正しくセット）、期限日はセル内日付input、目標/プロジェクトは1つの`Select`に`SelectGroup`で統合（目標選択でgoalIdセット・projectIdクリア、プロジェクト選択で逆、を実施）
- タグでタブ検索したい → `/search`を新規作成。全タスクからタグ一覧を抽出し、shadcn`Tabs`で「すべて」＋各タグを切り替え、選択中タグでタスクを絞り込み`TaskItem`リスト表示。ナビに「検索」を追加（「タスク」の次）

### 動作確認
- `npm run build`成功（ESLintの未使用変数エラーを1件修正）
- devサーバーで`/`・`/search`・`/tasks`が未ログイン時に307リダイレクトされることを確認
- 実際の動作（Today表示、バリデーション、インライン編集、タスク編集ページの自動保存、タグ検索の絞り込み）はユーザーに確認依頼

### 次回への申し送り
1. 上記一式を実機（ブラウザ）で確認してもらう
2. 問題なければVercelへのデプロイ（環境変数の設定を含む）
3. AI機能は再検討事項（コスト判断待ち）。SNS投稿・分析機能は将来構想

## 追記8: 「今やるべき候補」に予定日/期限日超過タスクが出ないバグを修正

ユーザー報告: 「今やるべき候補の中に、予定日や期限日が過ぎているタスクが表示されていない」。

調査の結果、深刻な穴を発見: 予定日(scheduledDate)を過去に設定したまま未完了のタスクは、以下すべてから漏れて**どこにも表示されない**状態だった（dueDateが未設定または未来の場合）:
- `getOverdueTasks`: dueDateが無いと対象外
- `getTodayTasks`: scheduledDateが「今日」でないと対象外（過去の予定日は対象外）
- `getFocusSuggestions`: `!t.scheduledDate`条件により、scheduledDateが設定されていれば過去でも一律除外

原因は前回セッションで「予定日を設定済みのタスクは候補から除外してほしい」という要望に対し、`!t.scheduledDate`という条件で「設定済みかどうか」だけを見ており、「その予定日がまだ来ていないか（未来）」を見ていなかったこと。

### 修正
- `src/lib/priority.ts`の`getFocusSuggestions`: 除外条件を`!t.scheduledDate`から`!(t.scheduledDate && !isOverdue(t.scheduledDate))`に変更。つまり「予定日が今日以降で設定されている」タスクのみ除外し、「予定日が過去（先送りされたまま）」のタスクは候補に残るようにした
- `focusScore`: scheduledDateが過去の場合にスコアを加点する処理を追加（dueDate超過と同様の扱い）。以前の`isToday(scheduledDate)`加点は、そのケースがgetTodayTasks側で先に拾われるため実質到達しないコードだったので置き換え
- `src/components/today/focus-suggestions.tsx`: 予定日超過で候補に出ているタスクには「予定 {日付}（未完了）」の赤字表示を追加し、なぜ候補に出ているか分かるようにした

### 動作確認
- `npm run build`成功
- devサーバーで`/`が未ログイン時に307リダイレクトされることを確認
- 実際の見た目（予定日超過タスクが候補に出ること）はユーザーに確認依頼

### 次回への申し送り（この後さらに1件バグ修正。下の追記を参照）

## 追記9: 「明日に延期」で今日やることから消えないバグを修正

ユーザー報告: Todayの「今日やること」で「明日に延期」を押してもリストに残ったまま。

原因: 「明日に延期」は`dueDate`だけを明日に更新しており、`scheduledDate`（実施予定日）が今日のままだと、`getTodayTasks`が`isToday(dueDate) || isToday(scheduledDate)`で判定しているため、scheduledDate側の条件で居座り続けていた。

### 修正
- `src/lib/priority.ts`に`getPostponeToTomorrowPatch(task)`を新設。dueDateは常に明日にし、scheduledDateが「今日以前」に設定されている場合はそれも明日に押し出す（scheduledDateが既に未来の場合は触らない）
- `src/components/task/task-item.tsx`（Today他）、`src/components/task/task-table-row.tsx`（タスク一覧）、`src/components/task/overdue-task-item.tsx`（期限切れ）の3箇所すべての「明日に延期」をこの共通関数に差し替え、ロジックの重複と食い違いを解消

### 動作確認
- `npm run build`成功
- devサーバーで`/`が未ログイン時に307リダイレクトされることを確認
- 実際の動作（「明日に延期」を押すと今日やることから消えること）はユーザーに確認依頼

### 次回への申し送り（この後さらに1件対応。下の追記を参照）

## 追記10: 目標/プロジェクトカラムの分離とデータ整合性ルールの実装

ユーザー指摘: タスク一覧の「目標/プロジェクト」を1つのカラムに統合するべきではない。加えて「プロジェクトに紐づくタスクは、そのプロジェクトの目標に一意に決まるはず」という重要なデータモデル上の整合性ルールを指摘された（プロジェクトが目標に属している以上、タスクが独自に別の目標を持てるのはおかしい）。

### 対応
「プロジェクトを選ぶとタスクの目標はそのプロジェクトの目標に自動的に決まり、独立して選べなくなる。プロジェクト未設定の時だけ目標を自由選択できる」というルールを、目標/プロジェクトのUIがある3箇所すべてに一貫して実装:

- `src/components/task/task-table-row.tsx`: 統合していた1セレクトを「目標」「プロジェクト」の2セルに分割。プロジェクト設定時、目標セルはSelectではなく読み取り専用テキスト（プロジェクトの目標を表示）にする。プロジェクト変更時は`goalId`もプロジェクトの`goalId`に自動同期
- `src/app/(app)/tasks/page.tsx`: テーブルヘッダーも「目標」「プロジェクト」の2列に分割
- `src/components/task/task-form-dialog.tsx`（新規作成）: 目標Selectをプロジェクト設定時は`disabled`にし、「プロジェクトの目標から自動的に決まります」と表示。プロジェクト選択時に目標を自動同期。プロジェクト一覧は目標での絞り込みをやめ全件表示に変更（旧: 目標→プロジェクト絞り込みという逆方向のUXだったが、データの向き（プロジェクト→目標が正）に合わせて廃止）
- `src/app/(app)/tasks/[id]/page.tsx`（編集ページ）: 同様に目標Selectを`disabled`化し、プロジェクトの目標を動的に表示・同期

いずれも「プロジェクトをなしに戻した場合、目標の値はそのまま保持（クリアしない）」という挙動に統一し、ユーザーがすぐに目標を選び直せるようにした。

### 動作確認
- `npm run build`成功
- devサーバーで`/tasks`が未ログイン時に307リダイレクトされることを確認
- 実際の動作（プロジェクト選択で目標が自動的に決まりロックされること）はユーザーに確認依頼

### 次回への申し送り（この後さらに1件UX修正。下の追記を参照）

## 追記11: 目標⇄プロジェクトのカスケード選択への作り直し

ユーザーから2点の指摘:
1. 目標を選んだ状態でプロジェクトを選ぶとき、その目標と無関係なプロジェクトまで選択肢に出てしまう（目標に属するプロジェクトだけに絞ってほしい）
2. プロジェクトを選んだ時、目標欄が「ロックされた表示専用フィールド」になるのはイメージと違う。目標かプロジェクトかの該当する選択肢だけがプルダウンに出て選べる形にしてほしい（disabledで固定するのではなく、選択肢自体を絞り込む）

前回実装（`disabled`でロック＋読み取り専用テキスト表示）を撤回し、双方向カスケード選択に作り直した。

### 実装
- `src/lib/task-links.ts`を新設: `getGoalSelectOptions(goals, projects, projectId)`（プロジェクトが選ばれていればそのプロジェクトの目標のみ、プロジェクトに目標が無ければ「なし」のみ、プロジェクト未選択なら全目標+なし）と`getProjectSelectOptions(projects, goalId)`（目標が選ばれていればその目標のプロジェクトのみ+なし、未選択なら全プロジェクト+なし）
- 目標Select・プロジェクトSelectはどちらも常に有効な通常のプルダウンとして描画し、`disabled`や読み取り専用テキストは廃止。選択肢自体を上記関数で絞り込むことで、矛盾する組み合わせがそもそも選べないようにした
- `src/components/task/task-form-dialog.tsx`（新規作成）、`src/app/(app)/tasks/[id]/page.tsx`（編集ページ）、`src/components/task/task-table-row.tsx`（一覧の簡易編集）の3箇所に同じロジックを適用
- プロジェクト選択時は引き続き目標を自動同期（`goalId: selected?.goalId`）。プロジェクトを「なし」に戻した時は目標をクリアせず保持し、その時点で目標Selectの選択肢が全目標に戻るので自由に選び直せる

### 動作確認
- `npm run build`成功
- devサーバーで`/tasks`が未ログイン時に307リダイレクトされることを確認
- 実際の動作（目標選択でプロジェクトの選択肢が絞られること、プロジェクト選択で目標の選択肢が1つに絞られること）はユーザーに確認依頼

### 次回への申し送り（この後さらに1件UI追加。下の追記を参照）

## 追記12: 今日の習慣に紐づく目標を薄い文字で表示

ユーザー要望: Today画面「今日の習慣」の各習慣に、どの目標のための習慣なのかを薄い文字で表示してほしい。

`src/components/today/habits-today.tsx`にgoalsストアを追加購読し、各習慣の`goalId`から対応する目標を引いて、習慣タイトルの下に`text-xs text-muted-foreground`（薄いグレー）で目標名を表示するようにした。目標が紐づいていない習慣にはこの行を出さない。

### 動作確認
- `npm run build`成功
- devサーバーで`/`が未ログイン時に307リダイレクトされることを確認
- 実際の見た目はユーザーに確認依頼

### 次回への申し送り
1. 上記の修正を実機で確認してもらう
2. 問題なければVercelへのデプロイ（環境変数の設定を含む）
3. AI機能は再検討事項（コスト判断待ち）。SNS投稿・分析機能は将来構想

## 追記13: 単発の「予定」（Schedule/Events）機能を追加

ユーザー要望: 通院や美容室のように不定期に発生する単発の予定を管理したい（既存の「習慣」＝繰り返し、「朝の時間割ブロック」＝今日固定の時間割、とは別物）。ヒアリングの結果、Todayページ最上部に直近5件をプレビューするウィジェットを置き、予定自体の追加・編集・削除は専用ページ`/schedule`で行う設計に決定。

### 実装
- `supabase/schema.sql`に`events`テーブルを追記（`title`必須、`date`必須、`time`/`location`/`notes`は任意、RLS・updated_atトリガーは他テーブルと同様）。**ユーザーはまだSQL Editorでこの追記分を実行していない** — 次回、追記11以降の末尾ブロック（`-- 追記（予定機能用）`）だけを実行してもらう必要がある
- `src/types/index.ts`に`Event`型を追加（`BaseEntity`拡張）
- `src/lib/supabase/queries/events.ts`: `createEntityQueries<Event>("events", { orderBy: "date" })`
- `src/store/useAppStore.ts`: `events`をstateに追加、`loadAll()`で他エンティティと並列fetch、`addEvent`/`updateEvent`/`removeEvent`を`addNote`等と同じtry/catch＋toast.errorパターンで実装（挿入・更新のたびにクライアント側で`date`昇順に再ソート）
- `src/components/event/event-form-dialog.tsx`・`event-row.tsx`: `HabitFormDialog`/`HabitRow`と同じ「作成編集兼用ダイアログ＋カード一覧」パターン
- `src/app/(app)/schedule/page.tsx`: 一覧ページ（日付昇順、追加ボタン、0件時はEmptyState）
- `src/components/today/upcoming-events.tsx`: 今日以降のイベントを日付昇順で最大5件、0件なら`HabitsToday`に倣い非表示（`return null`）。「すべて見る」リンクで`/schedule`へ
- `src/app/(app)/page.tsx`: 3カラムgridの外側・最上部（PageHeaderの直下）に`<UpcomingEvents />`を配置
- `src/components/layout/nav-links.tsx`: NAV_ITEMSに「予定」（`/schedule`、`CalendarDays`アイコン）を「習慣」の直前に追加

### 動作確認
- `npm run build`成功（型エラーなし、`/schedule`が静的ページとして生成されることを確認）
- devサーバーで`/schedule`が未ログイン時に307で`/login`へリダイレクトされることを確認
- 実際の見た目・DB反映はユーザーに確認依頼（下記の申し送り参照）

### 次回への申し送り
1. **`supabase/schema.sql`末尾の`events`テーブル追記分をSupabase SQL Editorで実行してもらう**（まだ未実行）
2. 実行後、`/schedule`で予定を追加し、Todayページ上部に表示されることを実機確認してもらう
3. Vercelデプロイの環境変数設定（前回からの継続タスク、進捗未確認）
4. AI機能は再検討事項（コスト判断待ち）。SNS投稿・分析機能は将来構想
